//Short-circuit Evaluation 
//conditional rendering in JSX && -> render right side only when left is true. || is the fallback pattern

// condition variable
let isLoggedin = 

//AND (&&) -> runs only if condition is true
isLoggedin && console.log("Welcome!!");

//OR (||) -> 